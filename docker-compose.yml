version: '3.3'
services:
  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: 'myapp'
      MYSQL_USER: 'myapp'
      MYSQL_PASSWORD: 'senha123'
      MYSQL_ROOT_PASSWORD: 'senha123'
    ports:
      - '3306:3306'
    expose:
      - '3306'
    # volumes:
    #   - my-db:/var/lib/mysql

# volumes:
#   my-db: